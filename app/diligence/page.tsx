import ModulePlaceholder from "@/components/ModulePlaceholder";

export default function DiligencePage() {
  return (
    <ModulePlaceholder
      eyebrow="Bulk extraction · doc × prompt grid"
      title="Diligence"
      sub="Rows are documents, columns are extraction prompts — run structured extractions across thousands of contracts."
      note="Diligence is part of the custom phase, after Research / Assistant / Dashboard land."
    />
  );
}
