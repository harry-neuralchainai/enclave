import ModulePlaceholder from "@/components/ModulePlaceholder";

export default function PlaybooksPage() {
  return (
    <ModulePlaceholder
      eyebrow="Codified firm standards"
      title="Playbooks"
      sub="Author rules with preferred / acceptable / fallback positions, severity, escalation, and trigger language."
      note="Playbooks are part of the custom phase, consumed by Review, Assistant, and Agents."
    />
  );
}
